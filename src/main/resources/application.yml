spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/database2
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true